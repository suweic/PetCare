# =============================================================================
# PetCare Web (用户端) — 生产环境变量
# =============================================================================
# 生产环境下所有请求通过 Nginx 反向代理，使用相对路径即可
# 如果前后端分离部署（非同一域名），请修改为完整 URL

VITE_API_BASE_URL=/api
VITE_WS_URL=/ws
VITE_APP_TITLE=PetCare-宠物在线问诊
VITE_UPLOAD_MAX_SIZE=5242880
